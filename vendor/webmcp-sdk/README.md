Vendored build of `@nekuda/webmcp-sdk` 0.5.0 (built from the nekuda monorepo `sdk/` with `bun run build && bun run build:types`).
Temporary until the package is on npm (NEK-730) — then this directory dies and the app depends on the registry version.
